// my decision

function evenOrOdd (number) {
    if (number % 2) {
        return "Odd";
    } else {
        return "Even"
    }
}

// the best decision

const even_or_odd = (number) => (number % 2 === 0 ? "Even" : "Odd");